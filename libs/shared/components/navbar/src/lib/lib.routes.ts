import { Route } from '@angular/router';
import { NavbarComponent } from './navbar/navbar.component';

export const sharedComponentsNavbarRoutes: Route[] = [
  { path: '', component: NavbarComponent },
];
